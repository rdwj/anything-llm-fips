---
apiVersion: v1
kind: Service
metadata:
  name: anything-llm-fips
  labels:
    app: anything-llm-fips
    app.kubernetes.io/name: anything-llm-fips
    app.kubernetes.io/component: service
spec:
  selector:
    app: anything-llm-fips
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
  sessionAffinity: None
